{
  "name": "app",
  "version": "0.0.1",
  "description": "Test application for generating documents",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "prepack": "npm run build",
    "dev": "npx rimraf dist && tsc -w",
    "build": "npx rimraf dist && tsc",
    "start": "node ./dist/index.js"
  },
  "keywords": [],
  "author": "Vladislav D.",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.3",
    "typescript": "^5.0.4",
    "tsconfig": "workspace:*"
  }
}
